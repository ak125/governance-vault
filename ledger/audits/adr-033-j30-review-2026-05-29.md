---
type: audit
status: complete
date: 2026-05-29
auditor: agent-autonome
related_adr: ["ADR-033"]
audit_trigger: "J+30 revue planifiée (ADR-033 §'Revue planifiée')"
repos_audited:
  - ak125/governance-vault
  - ak125/automecanik-wiki
  - ak125/nestjs-remix-monorepo
---

# ADR-033 J+30 Review — 2026-05-29

## Verdict global

**PARTIAL**

L'infrastructure technique (schéma frontmatter, template v2.0.0, validateur CI Python, workflow GitHub Actions, export diag-canon-slugs.json) est déployée et fonctionnelle. Deux blockers empêchent le verdict PASS : (1) ADR-033 est encore `status: proposed` dans le vault alors que tous les documents monorepo le déclarent `accepted 2026-04-29` — incohérence gouvernance G3 non résolue ; (2) la migration Phase 4 des 500+ fiches gamme n'a pas commencé (`wiki/gamme/` contient uniquement un `.gitkeep`).

---

## Critères de succès ADR-033 (9 points)

| # | Critère (ADR-033) | Status | Evidence |
|---|---|---|---|
| 1 | `_meta/schema/entity-data/gamme.schema.json` contient bloc `diagnostic_relations` | ⚠️ DÉVIATION | La propriété `diagnostic_relations` est dans `_meta/schema/frontmatter.schema.json` (top-level, conforme §D1), pas dans `entity-data/gamme.schema.json` (v2.2.0). Architecturalement correct, critère textuel non satisfait. SHA frontmatter : `921d4b38`. |
| 2 | `rag/docs/GAMME_PAGE_CONTRACT.md` en version 2.0 | ✅ PASS (indirect) | PR-A.rag mergée commit `224e4c63` le 2026-04-30. Documentée dans `workspaces/wiki/CLAUDE.md` et `workspaces/wiki/.claude/rules/wiki-batch.md` (monorepo). Repo `automecanik-rag` hors scope direct de cet audit. |
| 3 | `template/new-gamme.md` reflète `diagnostic_relations[]`, sans `diagnostic.symptoms[]` | ✅ PASS | `_meta/templates/gamme.md` `template_version: 2.0.0` — contient exemple `diagnostic_relations[]` complet avec defaults §D4 (`reviewed: false`, `diagnostic_safe: false`). Aucune occurrence `diagnostic.symptoms`. SHA : `1b5ffb27`. |
| 4 | `grep -rn "^  symptoms:" wiki/gamme/*.md` → 0 | ✅ PASS (vacuité) | `wiki/gamme/` contient uniquement `.gitkeep` — zéro fiche gamme. Aucune violation possible. Migration non démarrée. |
| 5 | `find wiki/systemes -type f \| wc -l` → 0 | ✅ PASS | Répertoire `wiki/systemes/` inexistant dans le repo (`wiki/` liste : constructeur, diagnostic, gamme, support, vehicle). |
| 6 | `find wiki/diagnostic -type f \| wc -l` → 0 | ⚠️ PARTIEL | `wiki/diagnostic/` contient 5 fichiers : `faq.md`, `safety-config.md`, `signs.md`, `vocab-clusters.md`, `wizard-steps.md`. Aucun ne correspond au pattern `<symptom>-*.md` — D3 non violé. Mais le critère strict (count=0) n'est pas atteint. |
| 7 | CI validateur en place + bloquant pour 3 anti-patterns D3 + FK inexistants | ✅ PASS | `scripts/wiki/validate-gamme-diagnostic-relations.py` déployé (SHA `4cf52ce3`). `.github/workflows/wiki-validate.yml` déployé (SHA `d076b1da`), sans `continue-on-error: true`, triggered sur PR touchant `workspaces/wiki/**` et `scripts/wiki/**`. `export-diag-canon-slugs.py` + `diag-canon-slugs-export.yml` déployés. |
| 8 | Lors du flip v2 : chaque fiche migrée avec `evidence.reviewed=false` et `evidence.diagnostic_safe=false` par défaut | ✅ N/A | Aucune fiche gamme dans `wiki/gamme/`. Critère applicable uniquement lors de la migration Phase 4. |
| 9 | Toute fiche `diagnostic_safe=true` a un commit signé reviewer ≠ auteur | ✅ N/A | Aucune fiche avec `diagnostic_safe: true` détectée (répertoire vide). |

**Score : 5 PASS + 2 N/A + 2 DÉVIATION = PARTIAL**

---

## Anti-patterns D3 (3 figés)

| Anti-pattern | Status | Evidence |
|---|---|---|
| `wiki/systemes/<slug>.md` | ✅ PASS | Répertoire `wiki/systemes/` inexistant. |
| `wiki/diagnostic/<symptom>-*.md` | ✅ PASS | `wiki/diagnostic/` contient uniquement des docs générales (`faq.md`, `safety-config.md`, `signs.md`, `vocab-clusters.md`, `wizard-steps.md`) — aucun fichier par-symptôme. Pattern `<symptom>-*.md` absent. |
| Réécriture/extension moteur diagnostic | ✅ PASS | Aucune modification détectée sur `__diag_*`, RPCs `kg_diagnose_*`, routes `/diagnostic-auto/*`. |

**D3 : 0 violation sur 3 anti-patterns.**

---

## ADR-033 status — BLOCKER

**Status en vault : `proposed`** (frontmatter `ADR-033-wiki-gamme-diagnostic-relations-contract.md`, SHA `08bd8206`).

**Status attendu : `accepted`** — tous les documents monorepo et le vault PR #108 (commit `77085ef9`) traitent ADR-033 comme accepté le 2026-04-29 :

| Source | Contenu |
|---|---|
| `workspaces/wiki/CLAUDE.md` (monorepo) | `ADR-033 (vault, accepted 2026-04-29, PR #108 commit 77085ef)` |
| `workspaces/wiki/.claude/rules/wiki-batch.md` | `ADR-033 : ... (status accepted 2026-04-29, PR vault #108 commit 77085ef)` |
| `.github/workflows/wiki-validate.yml` | `ADR-033 vault PR #108 commit 77085ef` |
| `workspaces/wiki/.claude/skills/wiki-proposal-writer/SKILL.md` | référence ADR-033 v2.0.0 comme canon |

La mise à jour du frontmatter vault (`status: proposed` → `status: accepted`, `decision_date: 2026-04-29`) n'a pas été faite lors du merge PR #108.

**Action requise P0** : PR vault séparée pour flipper `status: accepted` + `decision_date: 2026-04-29` dans ADR-033 et aligner MOC-Decisions.

---

## Migration Phase 4 — 0%

`wiki/gamme/` ne contient qu'un `.gitkeep`. La migration des 500+ fiches gamme (Phase 4 ADR-033 §"Implémentation") n'a pas commencé à J+30.

| Métrique | Valeur |
|---|---|
| Fiches avec `diagnostic_relations:` | 0 |
| Fiches avec anti-pattern `^  symptoms:` | 0 |
| Fiches gamme totales dans wiki/gamme/ | 0 (répertoire vide) |
| Progression | 0 / 500+ = 0% |

**Note** : les fiches gamme "proposals" sont dans `proposals/` (workspace monorepo), pas encore promues dans `wiki/gamme/`. Le pilote `audit/pilot-filtre-a-air-2026-05-26.md` (monorepo) documente 1 fiche pilote avec `diagnostic_relations_bien_formees: PASS` — mais cette fiche n'est pas encore promue en `wiki/gamme/`.

---

## Audit `diagnostic_safe=true`

**Aucune fiche à auditer.** `wiki/gamme/` vide → zéro occurrence de `diagnostic_safe: true` dans le wiki canon.

Les defaults §D4 sont respectés dans tous les artefacts de production :
- Template `gamme.md` : `diagnostic_safe: false` ✅
- Skill `wiki-proposal-writer` : defaults conservateurs §D4 documentés ✅
- `promote-raw-gammes-to-wiki.py` : `"diagnostic_relations": []` par défaut ✅

---

## Fraîcheur `exports/diag-canon-slugs.json`

**Stale : dernier export 2026-05-01 (28 jours).**

Le cron `diag-canon-slugs-export.yml` (schedule `0 3 * * 1` = lundi 03:00 UTC) n'a produit qu'un seul commit sur ce fichier depuis sa création. Les exécutions du 2026-05-04, 2026-05-11, 2026-05-18, 2026-05-25 semblent ne pas avoir produit de nouveau commit (soit le cron a échoué, soit le fichier était identique).

Le fichier actuel contient 64 symptômes sur 13 systèmes — cohérent avec les données DB audit 2026-04-29 (62-65 symptômes). Pas de signe de désynchronisation fonctionnelle, mais la fraîcheur doit être vérifiée.

`wiki-readiness-check.py` alerte si freshness > 7j. Le seuil est dépassé de 21 jours.

---

## Recommandations / Next steps

### P0 — ADR-033 vault status update (bloquant G3)

Ouvrir une PR vault séparée :
1. `ADR-033-wiki-gamme-diagnostic-relations-contract.md` : `status: proposed` → `status: accepted`, `decision_date: 2026-04-29`
2. `ops/moc/MOC-Decisions.md` : aligner le tableau (colonne Status = Accepted)

### P1 — Démarrer migration Phase 4 (milestone principal)

Exécuter le script `scripts/wiki/migrate-symptoms-to-relations.ts` (PR-E ADR-033) en mode `--dry-run --per-system freinage` sur les ~20 fiches gamme freinage. Vérifier le mapping label → `symptom_slug` avant écriture. Le pilote `filtre-a-air` est déjà documenté et validé (`audit/pilot-filtre-a-air-2026-05-26.md`).

### P2 — Investiguer le cron `diag-canon-slugs-export.yml`

Vérifier pourquoi le cron n'a pas produit de commit depuis 2026-05-01. Options :
- Logs GitHub Actions (exécutions silencieuses si diff=0 est le comportement attendu)
- Ou cron en erreur silencieuse

### P3 — Clarifier le critère 1 (gamme.schema.json vs frontmatter.schema.json)

Le critère de succès #1 cite `entity-data/gamme.schema.json` mais l'implémentation correcte (§D1 : top-level frontmatter) est dans `frontmatter.schema.json`. Mettre à jour le texte de l'ADR lors du flip `status: accepted` pour refléter l'implémentation réelle et éviter confusion lors des prochains audits.

### P4 — Clarifier `wiki/diagnostic/` (critère 6)

Les 5 fichiers de docs générales (`faq.md`, `safety-config.md`, etc.) ne violent pas D3 mais font échouer le critère 6 (count=0). Clarifier si ces fichiers sont légitimes (documentation opérationnelle du système diagnostic) et si le critère doit être reformulé en `find wiki/diagnostic -name "<symptom>-*.md" | wc -l → 0`.

---

## Annexes

### Commandes équivalentes exécutées

```
# §1 ADR status
mcp__github__get_file_contents governance-vault/ledger/decisions/adr/ADR-033-*.md
→ status: proposed (SHA 08bd8206)

# §2 Anti-pattern D3
mcp__github__get_file_contents automecanik-wiki/wiki/systemes/
→ 404 (répertoire inexistant) ✅

mcp__github__get_file_contents automecanik-wiki/wiki/diagnostic/
→ [.gitkeep, faq.md, safety-config.md, signs.md, vocab-clusters.md, wizard-steps.md]
   0 fichiers pattern <symptom>-*.md ✅

# §3 Phase 1 — schema
mcp__github__get_file_contents automecanik-wiki/_meta/schema/entity-data/gamme.schema.json
→ v2.2.0 — properties: {pg_id, family, related_parts, intents, vlevel, kw_top, references,
   maintenance, dimensions, decision_brief} — PAS de diagnostic_relations

mcp__github__get_file_contents automecanik-wiki/_meta/schema/frontmatter.schema.json
→ v1.0.0 — property diagnostic_relations[] présente et complète (SHA 921d4b38) ✅

mcp__github__get_file_contents automecanik-wiki/_meta/templates/gamme.md
→ template_version: 2.0.0 — diagnostic_relations[] présent, pas de symptoms[] ✅

# §3 Phase 1 — monorepo PRs
mcp__github__search_pull_requests "ADR-033 repo:ak125/nestjs-remix-monorepo"
→ [résultats non filtrables, contexte dans docs monorepo]

mcp__github__search_code "GAMME_PAGE_CONTRACT repo:ak125/nestjs-remix-monorepo"
→ 8 résultats — PR-A.rag v2.0 référencée commit 224e4c63

# §4 Migration 500+ fiches
mcp__github__get_file_contents automecanik-wiki/wiki/gamme/
→ [.gitkeep] — 0 fiche gamme ⚠️

# §5 diagnostic_safe=true
# wiki/gamme/ vide → 0 occurrence

# §6 Validateur CI
mcp__github__search_code "validate-gamme-diagnostic repo:ak125/nestjs-remix-monorepo"
→ validate-gamme-diagnostic-relations.py EXISTS (SHA 4cf52ce3)
   wiki-validate.yml EXISTS (SHA d076b1da), blocking ✅
   export-diag-canon-slugs.py EXISTS ✅
   diag-canon-slugs-export.yml EXISTS ✅

mcp__github__get_file_contents automecanik-wiki/exports/diag-canon-slugs.json
→ EXISTS — 64 symptômes, 13 systèmes ✅

mcp__github__list_commits automecanik-wiki exports/diag-canon-slugs.json --perPage 3
→ 1 commit : 2026-05-01 automecanik-bot (dernier) ⚠️ stale 28j
```

### Artefacts clés vérifiés

| Artefact | Repo | SHA | Status |
|---|---|---|---|
| ADR-033-wiki-gamme-diagnostic-relations-contract.md | governance-vault | 08bd8206 | proposed (BLOCKER) |
| _meta/schema/frontmatter.schema.json | automecanik-wiki | 921d4b38 | PASS (diagnostic_relations présent) |
| _meta/schema/entity-data/gamme.schema.json | automecanik-wiki | 7247311c | DÉVIATION (pas de diagnostic_relations) |
| _meta/templates/gamme.md | automecanik-wiki | 1b5ffb27 | PASS (template v2.0.0) |
| exports/diag-canon-slugs.json | automecanik-wiki | d0fc117c | STALE (2026-05-01, 28j) |
| scripts/wiki/validate-gamme-diagnostic-relations.py | nestjs-remix-monorepo | 4cf52ce3 | PASS |
| .github/workflows/wiki-validate.yml | nestjs-remix-monorepo | d076b1da | PASS (blocking) |
| wiki/gamme/ | automecanik-wiki | — | VIDE (migration 0%) |
| wiki/systemes/ | automecanik-wiki | — | INEXISTANT (PASS D3) |
| wiki/diagnostic/ | automecanik-wiki | — | 5 fichiers généraux (PASS D3, PARTIEL critère 6) |
